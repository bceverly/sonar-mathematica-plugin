name: Code Quality Check

on:
  push:
    branches: [ "**" ]  # Run on all branches
  pull_request:
    branches: [ "**" ]  # Run on all PRs

jobs:
  lint:
    name: Zero-Tolerance Linting
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Check for Java compiler warnings (ZERO TOLERANCE)
        run: |
          echo "========================================="
          echo "JAVA COMPILER WARNING CHECK - ZERO TOLERANCE"
          echo "========================================="
          echo ""
          echo "Compiling with all warnings enabled..."
          ./gradlew compileJava compileTestJava --no-daemon --console=plain > /tmp/compile_output.log 2>&1

          # Count ALL warnings (deprecation, unchecked, etc.)
          TOTAL_WARNINGS=$(grep -c "warning:" /tmp/compile_output.log || echo "0")
          DEPRECATION_WARNINGS=$(grep -c "warning: \[deprecation\]" /tmp/compile_output.log || echo "0")
          UNCHECKED_WARNINGS=$(grep -c "warning: \[unchecked\]" /tmp/compile_output.log || echo "0")
          OTHER_WARNINGS=$((TOTAL_WARNINGS - DEPRECATION_WARNINGS - UNCHECKED_WARNINGS))

          echo ""
          echo "Deprecation warnings: $DEPRECATION_WARNINGS"
          echo "Unchecked warnings:   $UNCHECKED_WARNINGS"
          echo "Other warnings:       $OTHER_WARNINGS"
          echo "Total warnings:       $TOTAL_WARNINGS"
          echo ""

          if [ "$TOTAL_WARNINGS" -gt 0 ]; then
            echo "❌ FAILURE: Java compiler warnings detected!"
            echo ""
            echo "This repository enforces ZERO-TOLERANCE for ALL compiler warnings."
            echo "Even a single warning will fail the build."
            echo ""
            echo "==============================================="
            echo "WARNING DETAILS:"
            echo "==============================================="
            echo ""
            grep "warning:" /tmp/compile_output.log -B2 -A2 | head -80
            if [ "$TOTAL_WARNINGS" -gt 20 ]; then
              echo ""
              echo "... and $((TOTAL_WARNINGS - 20)) more warnings"
            fi
            echo ""
            echo "==============================================="
            echo "HOW TO FIX:"
            echo "==============================================="
            echo "1. Run locally: make lint"
            echo "2. Fix all warnings by updating code (not by disabling checks)"
            echo "3. Common fixes:"
            echo "   - Deprecation: Update to current API"
            echo "   - Unchecked: Add proper generic types (e.g., List<?> instead of raw List)"
            echo "   - Other: Follow compiler suggestions"
            echo "4. Verify: make lint should show ZERO issues"
            echo ""
            exit 1
          else
            echo "✅ SUCCESS: ZERO compiler warnings!"
            echo ""
            echo "🎉 All code is warning-free!"
            echo "✨ No deprecation, unchecked, or other warnings detected!"
            echo ""
          fi

      - name: Run Checkstyle on main sources
        id: checkstyle_main
        run: |
          echo "Running Checkstyle on main sources..."
          ./gradlew checkstyleMain --no-daemon --console=plain

      - name: Run Checkstyle on test sources
        id: checkstyle_test
        run: |
          echo "Running Checkstyle on test sources..."
          ./gradlew checkstyleTest --no-daemon --console=plain

      - name: Parse Checkstyle results (ZERO TOLERANCE)
        run: |
          echo "========================================="
          echo "CHECKSTYLE RESULTS - ZERO TOLERANCE MODE"
          echo "========================================="

          # Count violations in main sources
          MAIN_VIOLATIONS=0
          if [ -f build/reports/checkstyle/main.xml ]; then
            MAIN_VIOLATIONS=$(grep -c "<error" build/reports/checkstyle/main.xml || echo "0")
          fi

          # Count violations in test sources
          TEST_VIOLATIONS=0
          if [ -f build/reports/checkstyle/test.xml ]; then
            TEST_VIOLATIONS=$(grep -c "<error" build/reports/checkstyle/test.xml || echo "0")
          fi

          TOTAL_VIOLATIONS=$((MAIN_VIOLATIONS + TEST_VIOLATIONS))

          echo ""
          echo "Main sources violations: $MAIN_VIOLATIONS"
          echo "Test sources violations: $TEST_VIOLATIONS"
          echo "Total violations: $TOTAL_VIOLATIONS"
          echo ""

          if [ $TOTAL_VIOLATIONS -gt 0 ]; then
            echo "❌ FAILURE: Code quality check FAILED!"
            echo ""
            echo "This repository enforces ZERO-TOLERANCE for code quality violations."
            echo "Even a single Checkstyle warning will fail the build."
            echo ""
            echo "==============================================="
            echo "VIOLATION DETAILS:"
            echo "==============================================="

            # Show main violations if any
            if [ $MAIN_VIOLATIONS -gt 0 ]; then
              echo ""
              echo "📋 Main source violations ($MAIN_VIOLATIONS):"
              echo "---"
              grep "<error" build/reports/checkstyle/main.xml | head -20 || true
              if [ $MAIN_VIOLATIONS -gt 20 ]; then
                echo "... and $((MAIN_VIOLATIONS - 20)) more violations"
              fi
            fi

            # Show test violations if any
            if [ $TEST_VIOLATIONS -gt 0 ]; then
              echo ""
              echo "📋 Test source violations ($TEST_VIOLATIONS):"
              echo "---"
              grep "<error" build/reports/checkstyle/test.xml | head -20 || true
              if [ $TEST_VIOLATIONS -gt 20 ]; then
                echo "... and $((TEST_VIOLATIONS - 20)) more violations"
              fi
            fi

            echo ""
            echo "==============================================="
            echo "HOW TO FIX:"
            echo "==============================================="
            echo "1. Run locally: ./gradlew checkstyleMain checkstyleTest"
            echo "2. View full reports:"
            echo "   - build/reports/checkstyle/main.html"
            echo "   - build/reports/checkstyle/test.html"
            echo "3. Fix all violations before pushing"
            echo "4. Verify: ./gradlew check"
            echo ""

            exit 1
          else
            echo "✅ SUCCESS: ZERO violations detected!"
            echo ""
            echo "🎉 Code quality is perfect!"
            echo "✨ All checks passed with flying colors!"
            echo ""
          fi

      - name: Run full build (includes tests)
        run: |
          echo "Running full build with tests..."
          ./gradlew build --no-daemon --console=plain

      - name: Upload Checkstyle reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: checkstyle-reports
          path: |
            build/reports/checkstyle/
          retention-days: 30

      - name: Final status
        if: success()
        run: |
          echo "================================================"
          echo "✅ ALL CHECKS PASSED!"
          echo "================================================"
          echo ""
          echo "✓ Java compiler warnings: ZERO"
          echo "  - Deprecation warnings: ZERO"
          echo "  - Unchecked warnings: ZERO"
          echo "  - Other warnings: ZERO"
          echo "✓ Checkstyle violations: ZERO"
          echo "✓ Build: SUCCESS"
          echo "✓ Tests: PASSED"
          echo ""
          echo "Code quality standards maintained! 🚀"
          echo ""
